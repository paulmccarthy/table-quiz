extends layouts/main

block content
  .row.justify-content-center
    .col-md-6.text-center.py-5
      h1.display-1= status || 500
      p.lead= message || 'Something went wrong.'
      a.btn.btn-primary(href="/") Go Home
